# ─────────────────────────────────────────────────────────────────────────────
# COMPSCI 426 — Team Project compose.yml
#
# This file defines the Holmes investigation container and a shared network.
# Add your own services below the "Add your services here" section.
#
# Every service you add must be attached to `team-net` so it is reachable
# from holmes by service name.
#
# Quick reference:
#   Start everything:          docker compose up --build
#   Open a shell in holmes:    docker compose exec holmes bash
#   View logs:                 docker compose logs -f
#   Check health:              docker compose ps
# ─────────────────────────────────────────────────────────────────────────────

networks:
  team-net:
    driver: bridge

services:

  # ── Holmes — System Investigation Container ────────────────────────────────
  # A persistent container with curl, wget, jq, k6, psql, redis-cli, Node,
  # Python, bat, lsd, fd, ripgrep, Neovim + LazyVim, and more.
  #
  # Use it to inspect and test your services from inside the Docker network:
  #
  #   docker compose exec holmes bash
  #   curl http://order-service:3000/health | jq .
  #   psql postgres://user:pass@postgres:5432/mydb
  #   redis-cli -h redis ping
  #   k6 run /workspace/k6/sprint-1.js
  #
  # Your repo root is mounted at /workspace, so all k6 scripts and files
  # are accessible without copying them into the container.
  #
  # See holmes/README.md for a full tool reference.
  holmes:
    build:
      context: ./holmes
      dockerfile: Dockerfile
    container_name: holmes
    restart: unless-stopped
    volumes:
      - .:/workspace
    networks:
      - team-net
    environment:
      - TERM=xterm-256color
    # Holmes has no ports — access it via: docker compose exec holmes bash

  payment:
    build: ./payment
    container_name: payment
    restart: unless-stopped
    ports:
      - "3003:3001"          # expose on host for local testing
    networks:
      - team-net             # REQUIRED — makes it reachable from holmes
    environment:
      - DATABASE_URL=postgres://admin:supersecurepassword123@payment-db:5432/payments
    depends_on:
      payment-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  payment-db:
    image: postgres:16
    container_name: payment-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: supersecurepassword123
      POSTGRES_DB: payments
    volumes:
      - ./payment/schema.sql:/docker-entrypoint-initdb.d/schema.sql
      - payment-db-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d payments"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── Add your services here ─────────────────────────────────────────────────
  #

  fraud-db:
    image: postgres:16
    container_name: fraud-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: frauddb
      POSTGRES_USER: fraud
      POSTGRES_PASSWORD: fraud
    volumes:
      - fraud-db-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fraud -d frauddb"]
      interval: 5s
      timeout: 3s
      retries: 10
  
  fraud-worker:
    build: ./fraud-detection
    container_name: fraud-worker
    restart: unless-stopped
    networks:
      - team-net
    environment:
      - PORT=3000
      - SERVICE_NAME=fraud-worker
      - DATABASE_URL=postgres://fraud:fraud@fraud-db:5432/frauddb
      - REDIS_URL=redis://redis:6379
      - FRAUD_QUEUE_KEY=fraud:queue
      - FRAUD_DLQ_KEY=fraud:dlq
      - FRAUD_FLAGGED_CHANNEL=fraud:flagged
      - FRAUD_RESULT_CHANNEL=fraud:result
      - "FRAUD_RESULT_KEY_PREFIX=fraud:result:"
      - FRAUD_HIGH_PRICE_THRESHOLD=500
      - FRAUD_MAX_SEATS_THRESHOLD=6
      - FRAUD_CARD_ATTEMPT_LIMIT=4
    depends_on:
      fraud-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s

  analytics-db:
    image: postgres:16
    container_name: analytics-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: analytics_db
      POSTGRES_USER: analytics
      POSTGRES_PASSWORD: analytics
    volumes:
      - analytics-db-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U analytics -d analytics_db"]
      interval: 5s
      timeout: 3s
      retries: 10

  analytics:
    build: ./analytics
    container_name: analytics
    restart: unless-stopped
    networks:
      - team-net
    environment:
      - PORT=3000
      - SERVICE_NAME=analytics-worker
      - DATABASE_URL=postgres://analytics:analytics@analytics-db:5432/analytics_db
      - REDIS_URL=redis://redis:6379
      - PURCHASE_QUEUE_NAME=analytics:purchase:queue
      - BROWSE_QUEUE_NAME=analytics:browse:queue
      - DLQ_NAME=analytics:dlq
    depends_on:
      analytics-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    

  # Pattern for each service:
  #
  # your-service:
  #   build: ./your-service
  #   container_name: your-service
  #   restart: unless-stopped
  #   ports:
  #     - "3001:3001"          # expose on host for local testing
  #   networks:
  #     - team-net             # REQUIRED — makes it reachable from holmes
  #   environment:
  #     - DATABASE_URL=postgres://user:pass@postgres:5432/yourdb
  #     - REDIS_URL=redis://redis:6379
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 3
  #     start_period: 15s
  #
  notification:
    build: ./notification
    container_name: notification
    restart: unless-stopped
    ports:
      - "3002:3000"          # expose on host for local testing
    networks:
      - team-net             # REQUIRED — makes it reachable from holmes
    environment:
      - REDIS_URL=redis://redis:6379
      - NOTIFICATION_SUB_KEY=notification:pubsub
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ── Postgres template ──────────────────────────────────────────────────────
  #
  # postgres:
  #   image: postgres:16
  #   container_name: postgres
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: pass
  #     POSTGRES_DB: mydb
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   networks:
  #     - team-net
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U user -d mydb"]
  #     interval: 5s
  #     timeout: 3s
  #     retries: 5
  #
  # ── Events DB ───────────────────────────────────────────────────────────────
  
  events-db:
    image: postgres:16
    container_name: events-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: events-db
    volumes:
      - ./event-catalog/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
      - events-db-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d events-db"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
  
  # ── Redis template ─────────────────────────────────────────────────────────
  #
  redis:
    image: redis:7
    container_name: redis
    restart: unless-stopped
    networks:
      - team-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  event-catalog:
    build: ./event-catalog
    container_name: event-catalog
    restart: unless-stopped
    ports:
      - "3005:3005"
    networks:
      - team-net
    environment:
      - PORT=3005
      - REDIS_URL=redis://redis:6379
      - SERVICE_NAME=event-catalog
      - DATABASE_URL=postgres://user:pass@events-db:5432/events-db
      - ANALYTICS_BQUEUE_NAME=analytics:browse:queue
    depends_on:
      redis:
        condition: service_healthy
      events-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3005/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  users:
    build: ./users
    container_name: users
    restart: unless-stopped
    ports:
      - "3006:3006"
    networks:
      - team-net
    environment:
      - PORT=3006
      - SERVICE_NAME=users
      - DATABASE_URL=postgres://user:pass@users-db:5432/users-db
    depends_on:
      users-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3006/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  users-db:
    image: postgres:16
    container_name: users-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: users-db
    volumes:
      - ./users/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
      - users-db-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d users-db"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
      
  ticket-purchase:
    build: ./ticket-purchase
    container_name: ticket-purchase
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - team-net
    environment:
      - DATABASE_URL=postgres://user:pass@ticket-purchase-db:5432/ticket-purchase-db
      - REDIS_URL=redis://redis:6379
      - SERVICE_NAME=ticket-purchase
      - FRAUD_QUEUE_NAME=fraud:queue
      - ANALYTICS_PQUEUE_NAME=analytics:purchase:queue
      - WAITLIST_QUEUE_NAME=waitlist-jobs
      - NOTIFICATION_PUBSUB_NAME=notification:pubsub
      - EVENT_CATALOG_URL=http://event-catalog:3005
      - PAYMENT_URL=http://payment:3001
      - RETRIES=3
      - TTL_MIN=10
    depends_on:
      ticket-purchase-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  waitlist:
    build: ./waitlist
    container_name: waitlist
    restart: unless-stopped
    networks:
      - team-net
    environment:
      - REDIS_URL=redis://redis:6379
      - SERVICE_NAME=waitlist-worker
      - QUEUE_NAME=waitlist:queue
      - DLQ_NAME=waitlist:dlq
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  ticket-purchase-db:
    image: postgres:16
    container_name: ticket-purchase-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: ticket-purchase-db
    volumes:
      - ./ticket-purchase/schema.sql:/docker-entrypoint-initdb.d/schema.sql
      - ticket-purchase-db-data:/var/lib/postgresql/data
    networks:
      - team-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d ticket-purchase-db"]
      interval: 5s
      timeout: 3s
      retries: 5

  refund: 
    build: ./refund
    container_name: refund
    restart: unless-stopped
    ports:
      - "3001:3000"
    networks:
      - team-net
    environment:
      - PORT=3000
      - REDIS_URL=redis://redis:6379
      - SERVICE_NAME=refund
      - DATABASE_URL=postgres://user:pass@refund-db:5432/refund_db
      - ANALYTICS_RQUEUE_NAME=analytics:refund:queue
    depends_on:
      refund-db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  refund-db:
    image: postgres:16
    container_name: refund-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: refund_db
    volumes:
      - refund-db-data:/var/lib/postgresql/data
      - ./refund/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    networks:
      - team-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d refund_db"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s

# ── Named volumes ──────────────────────────────────────────────────────────────
# Declare a volume for each database here, e.g.:
#
# volumes:
#   postgres-data:
volumes:
  payment-db-data:
  fraud-db-data:
  analytics-db-data:
  ticket-purchase-db-data:
  refund-db-data:
  events-db-data:
  users-db-data:
